import 'package:flutter/material.dart';

class FavritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('favorites'),
    );
  }
}
